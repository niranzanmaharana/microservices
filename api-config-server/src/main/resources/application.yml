spring:
  application:
    name: api-config-server
  profiles:
    active: native

  cloud:
    config:
      server:
        git:
          uri: https://github.com/niranzanmaharana/photo-app-configuration
          username: niranzanmaharana
          password: token
          clone-on-start: true
          default-label: master
        native:
          searchLocations: file:///D:/workspace/udemy/microservices/properties

server:
  port: 8888
